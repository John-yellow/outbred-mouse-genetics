# User-supplied sample metadata table; one tab-delimited row per sample.
# Schema (no header row): raw_name<TAB>clean_name
# raw_name: directory name below RAW_DIR containing paired FASTQ files.
# clean_name: sample name used for cleaned reads and downstream files.
# Keep this tracked template free of real or placeholder individual IDs.
# For a private run, set SAMPLE_MAP in config/pipeline.local.env to a private table.
