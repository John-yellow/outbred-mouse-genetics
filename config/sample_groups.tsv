# User-supplied population table; one tab-delimited row per sample.
# Schema (no header row): sample_id<TAB>population<TAB>sex
# sample_id: identifier matching the VCF/PLINK sample identifier.
# population: one of the workflow population labels (ICR1, ICR2, or KM).
# sex: sample sex label retained as metadata for the private analysis table.
# Keep this tracked template free of real or placeholder individual IDs.
# For a private run, set SAMPLE_GROUPS in config/pipeline.local.env to a private table.
