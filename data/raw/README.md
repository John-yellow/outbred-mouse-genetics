# Raw reads

Place one sample directory here, containing paired-end FASTQ files, for example:

```text
data/raw/SAMPLE_ID/SAMPLE_ID_R1.fq.gz
data/raw/SAMPLE_ID/SAMPLE_ID_R2.fq.gz
```

Raw reads are ignored by Git and are not part of the public code repository.
